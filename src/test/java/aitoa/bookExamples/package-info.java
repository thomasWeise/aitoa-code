/**
 * In this package, we place code used to generate examples in
 * our book. These examples are not unit tests, but they do not
 * belong into the "productive" of the main packages either. We
 * put them here because if we label them as tests, they will not
 * be included in the actual jar.
 */
package aitoa.bookExamples;
